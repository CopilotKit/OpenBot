[Unit]
# A manual systemd restart is a multi-process stop/start transaction and cannot
# retain one flock FD across both halves. Operators must use the locked helper.
RefuseManualStop=yes

[Service]
Environment=OPENBOT_DEPLOYMENT_LOCK_FILE=/var/lock/openbot-deployment.lock
